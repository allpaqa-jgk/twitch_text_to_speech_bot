# AGENTS.md - Project Guidelines (twitch_text_to_speech_bot)

このファイルは、本リポジトリ固有の技術仕様・アーキテクチャ・検証手順・リリース規約を定めたものです。
※ 全プロジェクト共通の開発・対話プロセス（設計ファースト、サブエージェント活用によるコスト最適化等）はグローバル設定（`~/.config/agents/AGENTS.md`）に準拠してください。

---

## 1. プロジェクト概要 & 技術スタック

- **ランタイム**: [Bun](https://bun.sh/) (TypeScript)
- **配信連携**: Twitch IRC (`tmi.js`) & Twitch OAuth
- **音声エンジン**: COEIROINK (v1/v2), VOICEVOX, Kokoro TTS, Piper TTS, macOS `say`
- **対象環境**: macOS, Windows, Linux

---

## 2. アーキテクチャ & 設計原則

1. **データドリブン & シンプルな拡張性**:
   - 特殊条件や文字列判定（ヒューリスティクス）を呼び出し元コードに散らばらせず、データ構造（配列やオブジェクト）側で完結する設計を優先すること。
   - 再生関数やキュー処理などは、データをシンプルにイテレートするだけの疎結合で保守性の高い状態を維持すること。
2. **キュー・音声プロセス制御**:
   - `TTSQueue` は FIFO で順次音声を再生し、`clear()` が呼ばれた際は待機中キューの破棄だけでなく、**現在スピーカーから再生中のプロセス（`afplay` / `powershell` / `say`）も即座に停止**すること。
3. **多言語カタカナ変換の自然なポーズ制御**:
   - OpenJTalk / COEIROINK では半角スペースが不要なポーズ（無音モーラ）やピッチリセットの原因となるため、変換後のカタカナ単語同士はスペースなしでシームレスに結合すること。
   - 息継ぎや文の区切りは日本語の句読点（`、`・`。`・`！`・`？`）によって自然に制御すること。
4. **安心・安全な辞書と言語表現**:
   - 配信でそのまま読み上げられるボットであるため、差別的・侮辱的・誤解を招く俗語やスラング（特に海外言語の罵倒語や不適切表現）は辞書・デモ文・テストケースに絶対に含めないこと。

---

## 3. テスト & ビルド検証手順

コードを変更した後は、必ず以下のテストおよびビルドを実行して安全性を確認すること。

- **単体テスト**:
  ```bash
  cd src && bun test
  ```
  ※ すべてのテストが 100% パスすることを確認する。
- **バイナリビルド**:
  ```bash
  cd src && bun run build
  ```
- **配布用スタンドアロンパッケージの生成**:
  ```bash
  bun run package
  ```

---

## 4. バージョン管理 & Git 運用

- **公開済みリリースの保護**:
  - GitHub 等ですでにリリース済みのタグ（例: `v2.1.1`）に対するコミットの `amend` や force-push は絶対に行わないこと。
  - 新しい改善や機能追加は、必ずパッチバージョンをインクリメント（例: `v2.1.2`）して新規コミット・タグ付け・リリースを行うこと。
- **バージョン番号の整合性**:
  - バージョンを更新する際は、ルートの `package.json` および `src/package.json` の両方を更新すること。
- **コミットメッセージ規約**:
  - Conventional Commits に従い、明確なプレフィックスを付けること（例: `feat(tts): ...`, `fix(cli): ...`, `refactor(demo): ...`）。
